'use client';

export default function AdminLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <div>
      <div>Admin Navigation - Replace with your AdminLayout.tsx content</div>
      {children}
    </div>
  )
}
